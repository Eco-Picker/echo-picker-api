package com.eco_picker.api.domain.newsletter.service

import com.eco_picker.api.global.data.properties.GeminiProperties
import org.springframework.stereotype.Service

@Service
class NewsletterService(private val geminiProperties: GeminiProperties) {
    fun publish() {}
    fun getNewsletters() {}
}